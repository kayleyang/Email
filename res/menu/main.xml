<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 自定义搜索按钮视图 
    <item
        android:id="@+id/main_menu_search"
        android:actionViewClass="android.widget.SearchView"
        android:showAsAction="ifRoom"
        android:title="@string/main_string_search"/>-->

    <!-- 刷新按钮图标 -->
    <item
        android:id="@+id/main_menu_refresh"
        android:icon="@drawable/ic_menu_refresh_holo_light"
        android:showAsAction="ifRoom"
        android:title="@string/main_string_refresh"/>

    <!-- 编辑按钮图标 -->
    <item
        android:id="@+id/main_menu_edit"
        android:icon="@android:drawable/ic_menu_edit"
        android:showAsAction="ifRoom"
        android:title="@string/main_string_edit"/>

</menu>